namespace ElympicsLobbyPackage.Blockchain
{
    public enum TransactionEvent
    {
        Sending,
        Sent,
        TransactionHash,
        Receipt,
        Confirmation,
        Error,
    }
}
